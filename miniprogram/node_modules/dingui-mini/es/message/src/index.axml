<view 
  class="
  {{$cssName}}
  {{$cssName}}__{{type}}
  {{$cssName}}-visible
  {{animate ? $cssName + '-visible__animated' : ''}}
  {{$cssName}}-visible-{{show ? 'in' : 'out'}}
  {{className}}"
>
  {{text}}
</view>