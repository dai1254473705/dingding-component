var slice = [].slice;

export default function (target) {
  var args = slice.call(arguments, 1);
  var ret = target;
  for (var i = 0; i < args.length; i++) {
    /* eslint eqeqeq:0 */
    if (ret == null) {
      return ret;
    }
    ret = ret[args[i]];
  }
  return ret;
}